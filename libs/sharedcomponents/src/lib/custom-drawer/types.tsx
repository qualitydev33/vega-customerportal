export type SelectedLinkType = {
    title: any;
    subMenu: boolean;
    openMenu: boolean;
    subMenuDefault: boolean;
    openMenuDefault: boolean;
};

export type MenuItemState = { title: string; subMenu: boolean; openMenu: boolean; subMenuDefault: boolean; openMenuDefault: boolean };
